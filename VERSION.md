## 规划

### 优化目标

- 按键优化
- 串口中断发送

### 版本映射

- [ ] 2.1 FatFs 文件系统移植
- [x] 2.0 PCB Y2 系列移植 + 步进电机驱动适配

- [ ] 1.9 细节处理与性能优化
- [x] 1.8 逻辑与文件整理
- [x] 1.7 mpu6050
- [x] 1.6 grayscale
- [x] 1.5 pid 
- [x] 1.4 wheel
- [x] Tim (timer, pwm, encoder)、key、oled、serial

## 样例
- `*`：未测试版本
- `@`：已知问题版本
- `!`：无法编译/烧录版本
~~- `v`：可编译烧录版本~~
~~- `u`：无法编译或烧录版本~~

## 版本

### 2.1.1
- `Imp` 优化 sdc 文件中一些细节问题
- `New` 新增 sdc 相关串口调用命令

### 2.1.0
- `New` 新增 FatFs 文件系统移植

### 2.0.8
- `Fix` 修复自动等待标志清零问题，现在可直接连续两次调用命令而无需担心数据冲突
- `Imp` 优化 Emm_V5 相关说明

### 2.0.7
- `Imp` 完善 Emm_V5 相关说明
- `Imp` 优化串口指令部分
- `Fix` 修复一些细节问题

### 2.0.6
- `Imp` 取消 Emm_V5_Get_Sys_Params 返回值，改为直接写入 zdtSysData_t 指针以简化调用

### 2.0.5
Features:
- `New` 新增张大头步进电机驱动代码 API 以便异步调用
- `Imp` 引入 stdbool 以替换手动 bool 类型
- `Imp` serial、Emm_V5 关键字简化

### 2.0.4
- `Imp` 完成适配张大头步进电机驱动代码

### 2.0.3!
- `tmp` 临时缓存版本，修复串口 p2p 中断发送问题

### 2.0.2!
- `tmp` 临时缓存版本，加入原始张大头步进电机驱动代码

### 2.0.1!
- `tmp` 临时缓存版本 

### 2.0.0
Features:
- `Imp` 移植至 PCB Y2 版本，适配新电路设计

Info:
- 由于 PCB Y2 设计缺陷，编码器无法使用，已暂停相关代码
- 若要使用编码器功能，请将接口焊至该 PCB 的上部（要舍弃按键 5、6）

---

### 1.9.2
Features:
- `Upd` 更新工程路径：
  - `app/kit` -> `app/core`
  - `app/work` -> `app/util`
  - 新增 `../module` 目录存放三方组件代码

### 1.9.1
Features:
- `Imp` 优化 mpu6050 工具函数及相关调用

### 1.9.0
Features:
- `Upd` 优化 I2C 软件模拟复用性
- `Imp` OLED 软件 I2C 复用

### 1.8.3
Features:
- `Upd` 整理 `app` 目录下文件分类
- `Upd` 更新 keil 工程编译路径（在此之前自最早合并后都无适配）

### 1.8.2
Features:
- `Imp` track 模式及速度控制

### 1.8.1
Features:
- `Imp` 变动代码文件位置，目录结构更清晰

### 1.8.0
Features:
- `优化` 梳理循迹逻辑结构，完善控制环生命周期
- `废弃` 旧指数步进 PID 代码

### 1.7.4
Features:
- `优化` 完成指数步进 PID 扩展版

Info:
方向环融合指数步进 PID 失败。保留指数步进 PID 扩展版代码。
**结论：** 短距离变化不适合使用指数步进 PID。

### 1.7.3
- `新增` 融合方向环
- `废弃` 单神经元 PID
- `优化` 可读化定时器初始化选项

### 1.7.2
- `优化` 格式化代码区域分割线

### 1.7.1
- `新增` mpu6050 偏航角计算

### 1.7.0
- `新增` 自定义软件模拟 I2C 驱动
- `新增` mpu6050 六轴传感器驱动及读取函数
- `优化*` 灰度传感器超限处理

### 1.6.0
- `新增` 灰度传感器滤波识别及量化函数（15档识别）
- `优化` 微调按键映射

### 1.5.4
- `优化` 封装指数步进算法，且范围升级（指数适配全 u8）

### 1.5.3
- `优化` 完成速度环指数步进 PID 调试

### 1.5.2@
- `测试` 1.5.1*
- `优化` 重构版本号命名规则

### 1.5.1*
- `优化` 废弃单神经元 PID，重写指数步进 PID（未测试）
- `修复` 解决 OLED 绘图只能绘制上半部分问题
- `新增` 动态 OLED UI 界面

### 1.5.0
- `优化` 项目结构，删去整合冗余文件代码
- `新建` 动态 pid（未测试）
  
### 1.4.4
- `修复` pwm 调速问题
- `优化` wheel 函数

### 1.4.3
- `新增` 重命名 rotate 为 wheel 电机控制部分
- `优化` 测试 timer 部分，修复 bug
- `修复` oled 上电不显示问题
- `问题` 貌似 pwm 无法调速？

### 1.4.2
- `优化` 完善定时器模式复用初始化（未测试）

### 1.4.1
- `新增` 定时器初始化 + pwm 初始化/设置

### 1.4.0!
- `新增` 移植先前 ch32v307 的定时器配置

### 1.3.0
- `优化` 格式化移植按键部分
- `优化` 移除 main 中所有外部变量及宏定义

### 1.2.3
- `优化` 串口函数注释及函数名

### 1.2.2
- `优化` 串口细节

### 1.2.1!
- `优化` 格式化 serial、oled 相关文件
- `修复` 编译时版本选择和标准库选择问题
- `问题` 无法用有线 DAP 烧录

### 1.2!
- `新增` 直接移植 carPro2 项目模板
- 本版本仅作引入暂存，未进行修改适配

### 1.1
- `优化` 修复无线烧录编译问题

### 1.0!
- `优化` 改变项目结构（有编译问题）

---
### 0.10
- `Upd` LICENSE 文件日期
- `Fix` README 标题错误
- `Fix` 文件 `stm32f4xx.h` 编码格式错误

### 0.9
- `优化` 开启 O2 优化选项 
- `优化` 整理代码结构：
  - 新建 `app` 文件夹存放应用逻辑代码
  - 移动 `user` 文件夹至 `sdk` 目录下（`main.c/.h` 移至 `app`，中断代码移至 `app/irq.c/.h`）
- `修复` `.clang-format` 应用语言设置错误
- `修复` 英文 README 中的格式错误

### 0.8
- `修复` 烧录失败问题

### 0.7
- `修复` 选型问题
- `优化` 除去 EIDE 冗余指令

### 0.6
- `修复` 开启外设驱动程序

### 0.5
- `修复` 中断文件 bug

### 0.4
- `优化` 完善相关文档

### 0.3
- `优化` 重构项目结构

### 0.2
- `优化` 在 `stm32f4xx.h` 中增加 BIT(x) 和 BITS(start, end) 宏定义 // 25.7.23

### 0.1
- `优化` 删去重复引用 // 25.7.23

### 0.0
- `修复` v0 更改晶振 25M -> 8M // 25.3.3
  - > [参考文档](https://blog.csdn.net/weixin_57941207/article/details/133945394)
