## 规划

- [ ] gpio
- [ ] pid 
- [x] wheel
- [x] Tim (timer, pwm, encoder)
- [x] key
- [x] oled 
- [x] serial 

## 样例
- `*`：未测试版本
- `@`：已知问题版本
~~- `v`：可编译烧录版本~~
~~- `u`：无法编译或烧录版本~~

## 版本

### 1.5.3
- `优化` 完成速度环指数步进 PID 调试

### 1.5.2@
- `测试` *1.5.1
- `优化` 重构版本号命名规则

### 1.5.1*
- `优化` 废弃单神经元 PID，重写指数步进 PID（未测试）
- `修复` 解决 OLED 绘图只能绘制上半部分问题
- `新增` 动态 OLED UI 界面

### 1.5.0
- `优化` 项目结构，删去整合冗余文件代码
- `新建` 动态 pid（未测试）
  
### 1.4.4
- `修复` pwm 调速问题
- `优化` wheel 函数

### 1.4.3
- `新增` 重命名 rotate 为 wheel 电机控制部分
- `优化` 测试 timer 部分，修复 bug
- `修复` oled 上电不显示问题
- `问题` 貌似 pwm 无法调速？

### 1.4.2
- `优化` 完善定时器模式复用初始化（未测试）

### 1.4.1
- `新增` 定时器初始化 + pwm 初始化/设置

### 1.4.0!
- `新增` 移植先前 ch32v307 的定时器配置

### 1.3.0
- `优化` 格式化移植按键部分
- `优化` 移除 main 中所有外部变量及宏定义

### 1.2.3
- `优化` 串口函数注释及函数名

### 1.2.2
- `优化` 串口细节

### 1.2.1!
- `优化` 格式化 serial、oled 相关文件
- `修复` 编译时版本选择和标准库选择问题
- `问题` 无法用有线 DAP 烧录

### 1.2!
- `新增` 直接移植 carPro2 项目模板
- 本版本仅作引入暂存，未进行修改适配

### 1.1
- `优化` 修复无线烧录编译问题

### 1.0!
- `优化` 改变项目结构（有编译问题）

---

### 0.8
- `修复` 烧录失败问题

### 0.7
- `修复` 选型问题
- `优化` 除去 EIDE 冗余指令

### 0.6
- `修复` 开启外设驱动程序

### 0.5
- `修复` 中断文件 bug

### 0.4
- `优化` 完善相关文档

### 0.3
- `优化` 重构项目结构

### 0.2
- `优化` 在 `stm32f4xx.h` 中增加 BIT(x) 和 BITS(start, end) 宏定义 // 25.7.23

### 0.1
- `优化` 删去重复引用 // 25.7.23

### 0.0
- `修复` v0 更改晶振 25M -> 8M // 25.3.3
> [doc](https://blog.csdn.net/weixin_57941207/article/details/133945394)
